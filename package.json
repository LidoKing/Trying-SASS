{
  "name": "tryingscss",
  "version": "1.0.0",
  "description": "Trying SCSS",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev-express": "sass --watch public/sass:public/css & node server.js",
    "dev-firebase": "sass --watch public/sass:public/css & firebase emulators:start",
    "watch": "sass --watch public/sass:public/css",
    "host": "firebase emulators:start",
    "deploy": "firebase deploy"
  },
  "author": "LidoKing",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "firebase": "^8.0.0"
  }
}
